#include "heuristic.h"
#include "board.h"
#include <cstdlib>

int		Heuristic::manhattan(const class Board &board)
{
	for (int i = 0; i < board.size(); i++)
	{

	}
	return (1);
	// return (abs(a.x - b.x) + abs(a.y - b.y));
}
